# Awards Photos

Replace these placeholder SVGs with award photos when ready. Keep the same filenames so the Awards page updates automatically:

- `lake-forest-senior-25.svg`
- `franklin-h-williams-award.svg`
- `chazen-institute-advanced-studies.svg`

You can also switch to `.jpg` or `.png`, but then update the matching `photo` field in `app/awards/page.tsx`.
